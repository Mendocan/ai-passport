# Release v0.3.1 — PowerShell memory namespace fix

## Summary

- `parseMemoryNamespaces` accepts **space-separated** lists (PowerShell comma quirk)
- `ai-passport grant cursor --memory preferences,projects,knowledge` works without quotes on Windows

## Install

```bash
npm install -g @ai-passport-core/cli@0.3.1
```

No other functional changes vs v0.3.0.
