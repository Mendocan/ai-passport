# Release v0.1.5 — Cloud sync prototype (RFC 0006)

## Summary

- `ai-passport sync push` / `pull` / `status` / `config`
- File provider: encrypted bundle to local directory (OneDrive/Dropbox folder compatible)
- Master key **never** uploaded — only ciphertext envelope + meta + grants
- Conflict detection via `updated_at` timestamps (`--force` to override)

## Install

```bash
npm install -g @ai-passport-core/cli@0.1.5
```

## Quick start

```bash
ai-passport sync config --target D:\Backups\ai-passport-sync
ai-passport sync push
ai-passport sync status
```

Restore (keys must still exist locally):

```bash
ai-passport sync pull
```

RFC: [0006-cloud-sync.md](https://github.com/Mendocan/ai-passport/blob/main/docs/rfcs/0006-cloud-sync.md)
